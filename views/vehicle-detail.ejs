<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <nav><%= nav %></nav>
  </header>
  <main>
    <% if (vehicleHtml) { %>
      <%= vehicleHtml %> <!-- Render the vehicle HTML if available -->
    <% } else { %>
      <p>Vehicle details not available.</p> <!-- Fallback message -->
    <% } %>
  </main>
</body>
</html>
